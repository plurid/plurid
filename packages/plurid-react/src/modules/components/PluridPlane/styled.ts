import styled from 'styled-components';



export const StyledPluridPlane: any = styled.div`
    position: absolute;
    height: auto;
    background-color: hsla(220, 10%, 40%, 0.4);
    width: 100%;
    /* width: ${(props: any) => {
        const width = props.viewSize.width * props.spaceScale;
        return width + 'px';
    }}; */
`;
