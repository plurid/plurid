import {
    StyledButton,
    StyledLinkButton,
} from './buttons';

import {
    StyledInputText,
    StyledSwitch,
} from './inputs';



export default {
    Button: StyledButton,
    LinkButton: StyledLinkButton,
    InputText: StyledInputText,
    Switch: StyledSwitch,
}
