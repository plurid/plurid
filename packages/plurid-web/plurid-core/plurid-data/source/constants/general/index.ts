// #region module
/**
 * WHERE PLURID LIVES. The chrome's help sheet says what the space is and where to read more, so
 * the two addresses are the data package's rather than a string in a component: a product that
 * renders its own help can name the same places.
 */
export const PLURID_SITE = 'https://plurid.com';
export const PLURID_REPOSITORY = 'https://github.com/plurid/plurid';

/**
 * WHAT A PLURID SPACE IS, in one paragraph, for a reader meeting one for the first time. It is
 * shown under the keys in the help sheet.
 */
export const PLURID_ABOUT = 'Plurid is an open framework for spatial interfaces on the web. A page is a plane standing in a real three-dimensional space, a link opens the plane it names beside the one it came from, and the camera does the travelling, so a digression can go behind the thing it came from and still be read.';

export const directions = {
    left: 'left',
    right: 'right',
    up: 'up',
    down: 'down',
};
// #endregion module
