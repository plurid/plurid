# `plurid dev` loads .env, .env.development and their .local variants; `plurid build` / `plurid start`
# load .env, .env.production and theirs. Copy this file to the one you want; none is required.
PORT=33721
